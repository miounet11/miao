# 🚀 Miaoda IDE - 快速启动指南

## 📦 安装

### 1. 安装依赖

```bash
cd /Users/lu/ide/miaoda-ide

# 安装各扩展依赖
cd extensions/agent-orchestrator && npm install
cd ../welcome-experience && npm install
cd ../onboarding && npm install
cd ../shared-services && npm install
```

### 2. 编译所有扩展

```bash
# Agent Orchestrator
cd extensions/agent-orchestrator && npm run compile

# Welcome Experience
cd ../welcome-experience && npm run compile

# Onboarding
cd ../onboarding && npm run compile

# Shared Services
cd ../shared-services && npm run compile
```

---

## 🎮 测试 Phase 1 功能

### 方法 1: VSCode Extension Development Host

1. 在 VSCode 中打开项目:
   ```bash
   code /Users/lu/ide/miaoda-ide
   ```

2. 按 `F5` 启动 Extension Development Host

3. 在新窗口中测试命令:
   - `Cmd+Shift+P` → 输入 "Miaoda"
   - 选择命令测试

### 方法 2: 命令面板测试

```
1. Cmd+Shift+P
2. 输入: "Miaoda: Show Welcome"
3. 观看 3 秒速度对比演示
```

---

## 🎯 核心功能测试清单

### ✅ 欢迎体验

```
命令: miaoda.showWelcome

预期结果:
- 显示欢迎页面
- 播放 Cursor vs Miaoda 动画
- 显示 "Miaoda 快 2.8x"
- 提供 "开始使用" 按钮
```

### ✅ 速度对比

```
命令: miaoda.showSpeedComparison
快捷键: Cmd+Shift+A (启动 Agent Team)

预期结果:
- 并排显示 Cursor 和 Miaoda
- 实时动画展示任务执行
- 计算并显示加速比
- 显示节省时间统计
```

### ✅ Agent 可视化

```
命令: miaoda.showAgentVisualization

预期结果:
- 显示 3 个 Agent 卡片
- 实时更新工作状态
- 显示进度条动画
- 显示实时指标
```

### ✅ 7 天引导

```
命令: miaoda.showDailyTasks

预期结果:
- 显示 Day 1 任务列表
- 显示任务进度 (0/3)
- 显示奖励信息
- 提供 "开始任务" 按钮
```

---

## 🎨 UI 预览

### 速度对比视图

打开后应该看到:
- 左侧: Cursor (串行执行)
- 右侧: Miaoda (并行执行)
- 实时计时器
- 任务进度条
- 最终结果统计

### Agent 可视化面板

打开后应该看到:
- 3 个 Agent 卡片 (Architect, Backend, Test)
- 每个 Agent 的状态和进度
- 顶部指标面板
- 动画效果

### 每日任务面板

打开后应该看到:
- Day 1/7 标题
- 3 个任务卡片
- 任务描述和指引
- 奖励信息
- 解锁功能提示

---

## 🐛 常见问题

### Q1: 编译失败

```bash
# 清理并重新编译
cd extensions/agent-orchestrator
rm -rf out node_modules
npm install
npm run compile
```

### Q2: 命令找不到

检查 `package.json` 中的 `contributes.commands` 是否正确注册。

### Q3: Webview 不显示

检查:
1. `enableScripts: true` 是否设置
2. HTML 内容是否正确
3. 浏览器控制台是否有错误

---

## 📊 性能指标

### 目标

- Webview 加载时间: < 500ms
- 动画帧率: 60 FPS
- 内存占用: < 50MB
- 命令响应时间: < 100ms

### 测试方法

```javascript
// 在 Webview 中测试
console.time('load');
// ... 加载内容
console.timeEnd('load');
```

---

## 🔧 开发工具

### VSCode 调试

1. 设置断点
2. 按 `F5` 启动调试
3. 在 Extension Host 中触发命令
4. 查看调试信息

### Webview 调试

1. 打开 Webview
2. `Cmd+Shift+P` → "Developer: Open Webview Developer Tools"
3. 查看控制台和网络请求

---

## 📝 下一步开发

### 优先级 1: 集成真实功能

- [ ] 连接真实 AI 模型
- [ ] 实现 Skill 生成
- [ ] 添加任务调度逻辑

### 优先级 2: 完善 UI

- [ ] 添加加载状态
- [ ] 优化动画性能
- [ ] 添加错误处理

### 优先级 3: 用户体验

- [ ] 添加快捷键提示
- [ ] 实现撤销功能
- [ ] 添加帮助文档

---

## 🎉 成功标志

当你看到以下内容时，说明 Phase 1 成功:

✅ 欢迎页面正常显示
✅ 速度对比动画流畅播放
✅ Agent 可视化实时更新
✅ 每日任务正确显示
✅ 所有命令正常响应
✅ 无编译错误
✅ 无运行时错误

---

**准备好了吗？按 F5 启动测试！**
